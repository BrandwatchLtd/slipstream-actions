{
  "name": "slipstream-action-push-image",
  "version": "1.0.0",
  "scripts": {
    "dev": "../node_modules/.bin/ncc build index.js -o dist -w",
    "build": "../node_modules/.bin/ncc build index.js -o dist",
    "test": "npm run lint && npm run unit",
    "unit": "../node_modules/.bin/jest",
    "lint": "../node_modules/.bin/eslint . --config ../.eslintrc.json --ignore-path ../.eslintignore"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/exec": "^1.0.4",
    "docker-cli-js": "^2.8.0"
  }
}
