name: Install CLI
description: Install Slipstream CLI on runner'
inputs:
  url:
    description: URL of the slipstream binary to download
    required: false
    default: https://raw.githubusercontent.com/BrandwatchLtd/slipstream-cli-release/master/install.sh
runs:
  using: node12
  main: dist/index.js
