name: Install CLI
description: Install Slipstream CLI on runner'
inputs:
  url:
    description: URL of the slipstream binary to download
    required: false
    default: https://github.com/BrandwatchLtd/slipstream-cli-release/releases/download/v0.4.0+slipstream/slipstream-cli-release_0.4.0+slipstream_linux_amd64.tar.gz
runs:
  using: node12
  main: dist/index.js
